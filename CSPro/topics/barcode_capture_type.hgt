<title>Barcode Capture Type</title>

The Barcode capture type starts an Android device's camera and allows the operator to scan a barcode. The barcode can be in <link https://developers.google.com/ml-kit/vision/barcode-scanning>several formats</link>, including QR codes. Note this capture type only appears on Android. On Windows a Text Box is displayed.

<center><image barcode.png /></center>

It is also possible to capture barcodes using the <logiccolor>barcode.read</logiccolor> logic function.

<seealso capture_types.hgt barcode_read_function.hgt />
