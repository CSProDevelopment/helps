<title>Close Function</title>
<context logic_help_close />

<subheader>Format</subheader>

<logicsyntax>
b = close(<arg>dictionary_name</arg> ‖ <arg>file_name</arg>);
</logicsyntax>

<subheader>Description</subheader>

The <logiccolor>close</logiccolor> function closes a file, whether opened as a dictionary or via a <logiccolor>file</logiccolor> statement. The function closes either a dictionary named <arg>dictionary_name</arg> or a file named <arg>file_name</arg>.

<subheader>Return Value</subheader>

The function returns a logical value of 1 (true) if successful and 0 (false) otherwise.

<subheader>Example</subheader>

<logic>
close(pffFile);
execpff(filename(pffFile));
</logic>

<seealso open_function.hgt setfile_function.hgt />
